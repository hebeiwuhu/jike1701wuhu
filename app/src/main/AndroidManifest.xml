<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schedule_he">

    <!--允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--获取GSM�dsds?g）、WCDMA（联�?g）等网络状态的信息  -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下�?-->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--获取sd卡写的权限，用于文件上传和下�?->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--允许读取手机状�?用于创建BmobInstallation-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_my"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.bianqian.EditActivity">

        </activity>

        <activity android:name=".ui.richeng.Edit_RCActivity">

        </activity>

        <activity android:name=".ui.kecheng.AddCourseActivity">

        </activity>
        <receiver android:name=".ui.richeng.AlarmReceiver">

        </receiver>
        <activity android:name=".ui.richeng.RC_xiangxiActivity">

        </activity>
        <service
            android:name=".BackGroundService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ui.user.LoginActivity">

        </activity>
        <activity android:name=".ui.user.RegisterActivity">

        </activity>
        <provider
            android:name="cn.bmob.v3.util.BmobContentProvider"
            android:authorities="com.example.schedule_he.BmobContentProvider">
        </provider>
        <activity android:name=".ui.user.UserInfoActivity">

        </activity>
    </application>

</manifest>